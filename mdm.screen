#!/bin/sh

TMPFILE=`mktemp -d -t mdm-XXXXXXXXXX` || exit 1
cat <<EOF > $TMPFILE/screenrc
screen mdm-worker $TMPFILE
screen mdm-worker $TMPFILE
screen mdm-worker $TMPFILE
screen mdm-worker $TMPFILE
EOF
mdm-core $TMPFILE < $1 &
screen -c $TMPFILE/screenrc
rm -Rf $TMPFILE
